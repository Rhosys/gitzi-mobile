package ch.rhosys.gitzi.ui.theme

import androidx.compose.ui.graphics.Color

// Brand seed — three kanban lanes, one color. Swap freely; nothing else depends on the exact hue.
val GitziIndigo = Color(0xFF4F46E5)
val GitziIndigoLight = Color(0xFF818CF8)

val StagePrioritized = Color(0xFF64748B)
val StageDesigning = Color(0xFF7C3AED)
val StageCoding = Color(0xFF2563EB)
val StageReviewing = Color(0xFFD97706)
val StageAuditing = Color(0xFFDB2777)
val StageDeploying = Color(0xFF0891B2)
val StageDone = Color(0xFF16A34A)
val StageBuffer = Color(0xFF94A3B8)
